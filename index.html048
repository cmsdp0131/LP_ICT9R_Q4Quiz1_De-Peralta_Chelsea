<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>BMI Calculator</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  
  <!-- Bootstrap CDN -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

  <!-- Google Font -->
  <link href="https://fonts.googleapis.com/css2?family=Rubik&display=swap" rel="stylesheet">

  <style>
    body {
      font-family: 'Rubik', sans-serif;
      background: linear-gradient(to right, #a8c0ff, #c1ffea);
      color: #333;
      padding: 2rem;
    }

    h1 {
      font-size: 2.5rem;
      font-weight: bold;
    }

    .bmi-section {
      display: flex;
      flex-wrap: wrap;
      align-items: center;
      justify-content: center;
    }

    .bmi-image {
      flex: 1 1 300px;
      text-align: center;
    }

    .bmi-image img {
      max-width: 250px;
    }

    .bmi-content {
      flex: 1 1 400px;
      padding: 1rem;
    }

    .form-control {
      font-size: 1.2rem;
      text-align: center;
    }

    .unit-label {
      font-size: 0.9rem;
      background-color: #333;
      color: #fff;
      display: inline-block;
      padding: 0.2rem 0.5rem;
      border-radius: 4px;
      margin-top: 0.2rem;
    }

    .btn-yellow {
      background-color: #ffca28;
      border: none;
      color: black;
      font-weight: bold;
    }

    .btn-yellow:hover {
      background-color: #ffc107;
    }

    .output {
      background-color: #212529;
      color: white;
      text-align: center;
      padding: 1rem;
      border-radius: 8px;
      margin-top: 2rem;
      font-size: 1.2rem;
    }
  </style>
</head>
<body>

  <div class="container bmi-section">
    <!-- BMI Image -->
    <div class="bmi-image">
      <img src="https://cdn-icons-png.flaticon.com/512/2696/2696063.png" alt="BMI Icon">
    </div>

    <!-- BMI Content -->
    <div class="bmi-content">
      <h1>BMI Calculator</h1>
      <p><strong>Body Mass Index (BMI)</strong> categories are standardized classifications used to assess weight status based on an individual's height and weight measured in kilogram per square meter (<i>kg/m²</i>).</p>

      <!-- Instructions Toggle -->
      <details ontoggle="console.log('Instructions toggled')">
        <summary><strong>▶ Instructions</strong></summary>
        <p class="mt-2">Enter your weight and height, click the button, and your BMI + health status will be revealed!</p>
      </details>

      <!-- BMI Form -->
      <form id="bmiForm" class="mt-4">
        <label for="weight" class="form-label">weight in kilogram</label>
        <input type="number" step="0.1" class="form-control" id="weight" required>
        <div class="unit-label">kilogram</div>

        <label for="height" class="form-label mt-3">height in meter</label>
        <input type="number" step="0.01" class="form-control" id="height" required>
        <div class="unit-label">meter</div>

        <button type="button" class="btn btn-yellow w-100 mt-4" onclick="calculateBMI()">Calculate BMI</button>
      </form>
    </div>
  </div>

  <!-- Output Section -->
  <div id="bmiResult" class="container output mt-5">Output</div>

  <script>
    function calculateBMI() {
      // Grab user input
      let weight = parseFloat(document.getElementById('weight').value);
      let height = parseFloat(document.getElementById('height').value);

      // Validation
      if (!weight || !height || weight <= 0 || height <= 0) {
        alert("Please enter valid numbers for both weight and height.");
        return;
      }

      // Calculate BMI
      let bmi = weight / Math.pow(height, 2);
      let category = "";

      // Determine category
      if (bmi < 18.5) {
        category = "Underweight 💨";
      } else if (bmi >= 18.5 && bmi <= 24.9) {
        category = "Normal weight 🧘‍♀️";
      } else if (bmi >= 25 && bmi <= 29.9) {
        category = "Overweight 🍕";
      } else {
        category = "Obese 🍩";
      }

      // Output result
      document.getElementById('bmiResult').innerHTML = 
        `Your BMI is <strong>${bmi.toFixed(2)}</strong> - <em>${category}</em>`;
    }
  </script>
</body>
</html>
